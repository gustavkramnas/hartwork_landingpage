import styled from 'styled-components'

export const BaseSection = styled.section`
  max-width: ${(props) => props.theme.style.layout.maxWidth};
`
export const BaseMain = styled.main`
  margin: 0 auto;
`
