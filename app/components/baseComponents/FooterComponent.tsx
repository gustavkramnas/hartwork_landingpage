import { theme } from '../../utils/appSettings/theme'

export const Footer = () => {
  return (
    <footer style={{ background: theme.style.colors.secondary }}>
      <p>contact</p>
    </footer>
  )
}
